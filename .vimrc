set number
set relativenumber
syntax on

set tabstop=4        " chaque tab = 4 colonnes
set shiftwidth=4     " indentation automatique = 4 colonnes
set softtabstop=4    " Tab et Backspace comptent pour 4
set noexpandtab      " utiliser des vraies tabs, pas des espaces

set autoindent       " répète l'indentation de la ligne précédente
set cindent          " indentation C stricte (mieux que smartindent)