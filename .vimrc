" vim, not nvim: the fallback editor on a machine where nvim is absent.
" Tabs, never spaces, four columns wide. Byte-identical across both repos.

set number
set noexpandtab
set tabstop=4
set shiftwidth=4
set softtabstop=0
