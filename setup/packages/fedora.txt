# dnf packages, one per line: packages.sh reads the FIRST word of every
# uncommented line. Sections mirror the Brewfile.
# Everything NOT in this file (installed by ./run, by mise, by hand, or
# deliberately absent) is in docs/packages.md: one table, both OSes.

# core: shell, multiplexer, vcs
git
tmux
zsh
bash
bash-completion

# modern CLI
tree
eza
zoxide
fzf
bat
fd-find
ripgrep
btop
jq

# build & tasks
make
cmake
just

# infra / automation
ansible
ansible-lint
podman
# Docker, from the BASE repos: moby-engine is the same upstream engine as
# docker-ce, packaged by Fedora, so it needs no third-party repo and no
# `confirm`. Never install podman-docker alongside it: both own /usr/bin/docker.
moby-engine
docker-compose
docker-buildx

# secrets / security
gnupg2
age
yubikey-manager
pcsc-lite
pcsc-lite-devel

# system integration
util-linux-user
wl-clipboard
xclip

# terminal
kitty

# browsers
firefox
