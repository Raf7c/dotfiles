# Fedora packages (dnf) — one name per line. # = comment, empty lines ignored.
# Read by install/packages.sh -> pkg_install. CLI mirror of the Brewfile (macOS).

git
zsh
bash
bash-completion
tmux
bat
fd-find          # provides the `fd` command
ripgrep
fzf
zoxide
eza

# --- not in the base repos: install another way (see packages) ---
# starship       # COPR atim/starship, or official script
# lazygit        # COPR atim/lazygit
# mise           # via recipe (curl https://mise.run | sh -> ~/.local/bin); also dnf repo mise
