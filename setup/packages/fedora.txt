# dnf packages, one per line: packages.sh reads the FIRST word of every
# uncommented line. Sections mirror the Brewfile.
# Everything NOT in this file — installed by ./run, by mise, by hand, or
# deliberately absent — is in docs/tools.md: one table, both OSes.

# core — shell, multiplexer, vcs
git
tmux
zsh
bash
bash-completion

# modern CLI
tree
eza
zoxide
fzf
bat
fd-find
ripgrep
btop
jq

# build & tasks
make
cmake
just

# infra / automation
ansible
ansible-lint
podman

# secrets / security
gnupg2
age
yubikey-manager

# system integration
util-linux-user
wl-clipboard
xclip

# terminal
kitty

# browsers
firefox
