# shell & core
brew "git"
brew "tmux"
brew "zsh"
brew "bash"
brew "bash-completion@2"
brew "starship"
brew "tree"
brew "eza"
brew "zoxide"
brew "fzf"
brew "bat"
brew "mise"

# CLI tools
brew "make"
brew "fd"
brew "ripgrep"
brew "lazygit"
brew "btop"
brew "jq"
brew "gnupg"
brew "just"

# Infra / automation
brew "ansible"
brew "ansible-lint"
brew "yamllint"
brew "cloudflared"   # macOS only (not installed on Fedora)

# Secrets / security (macOS only)
brew "age"                  # modern file encryption
brew "sops"                 # secrets in git (age/KMS backends)
brew "age-plugin-yubikey"   # age identities backed by a YubiKey
brew "ykman"                # YubiKey Manager CLI

# terminal
cask "ghostty"
cask "kitty"

# Tools Dev
# (claude code: installed via the official script in packages.sh, NOT brew —
#  the formula lags behind Anthropic's release pace)
cask "jetbrains-toolbox"
cask "gitkraken"
cask "docker-desktop"   # engine + CLI (the `docker` formula is CLI-only)

# productivity
cask "raycast"
cask "keymapp"
cask "obsidian"

# Ai
cask "claude"

# browser
cask "firefox"
cask "google-chrome"
